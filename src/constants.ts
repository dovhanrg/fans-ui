import * as process from "process";

export const apiServer = "https://funs.dovhan.com.ua"

export const authRoute = `${apiServer}/auth/login`
export const addUserRoute = `${apiServer}/api/v1/add-user`
